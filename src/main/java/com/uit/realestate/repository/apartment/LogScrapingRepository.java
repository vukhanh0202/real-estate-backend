package com.uit.realestate.repository.apartment;

import com.uit.realestate.domain.apartment.LogScraping;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogScrapingRepository extends JpaRepository<LogScraping, Long> {
}
