package com.uit.realestate.constant;

public interface MessageCode {
    interface User {
        String USER_NOT_FOUND = "message.user.NotFound";
    }
    interface Token {
        String INVALID_TOKEN = "message.token.InvalidToken";
    }
    interface Apartment {
        String APARTMENT_NOT_FOUND = "message.apartment.NotFound";
    }
}
